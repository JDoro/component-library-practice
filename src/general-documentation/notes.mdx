import {Meta} from "@storybook/blocks";

<Meta title="Notes" />

# Notes
- Don't forget `?raw` at the end of the URL to get the raw markdown content.
- This project has image snapshots for visual regression testing.
- This project has Accessibility tests.

